[project]
name = "maieuticanalyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "pandas>=3.0.1",
    "xlrd>=2.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
